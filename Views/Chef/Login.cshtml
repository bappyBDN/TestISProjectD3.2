@model HomeChefHub.Models.Chef

@{
    ViewData["Title"] = "Chef Login";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chef Login</title>
    <link rel="stylesheet" href="~/css/styles_login_chef.css">
</head>
<body>
    <div class="container">
        <!-- Left Panel with Branding -->
        <div class="left-panel">
            <div class="overlay">
                <p>Cook with passion, login to your account!</p>
            </div>
        </div>

        <!-- Right Panel with Login Form -->
        <div class="right-panel">
            <div class="form-box">
                <h2>LOGIN</h2>
                <p>Welcome back! Enter your credentials.</p>

                @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
                {
                    <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
                }

                <form asp-action="Login" method="post">
                    @Html.AntiForgeryToken()
                    <input type="email" name="email" placeholder="Email Address" required>
                    <input type="password" name="password" placeholder="Password" required>
                    <button type="submit">LOGIN</button>
                    <p class="register-link">New here? <a href="@Url.Action("Signup", "Chef")">Create an account</a></p>
                </form>
            </div>
        </div>
    </div>
</body>
</html>



@* @model HomeChefHub.Models.Chef

@{
    ViewData["Title"] = "Chef Login";
}

<div class="container mt-5">
    <h2 class="text-center">Chef Login</h2>

    @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
    {
        <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
    }

    <form asp-action="Login" method="post" class="w-50 mx-auto">
        @Html.AntiForgeryToken()

        <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input type="email" name="email" class="form-control" required />
        </div>

        <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input type="password" name="password" class="form-control" required />
        </div>

        <button type="submit" class="btn btn-primary w-100">Login</button>
    </form>

    <div class="text-center mt-3">
        <p>Don't have an account? <a href="@Url.Action("Signup", "Chef")">Sign Up</a></p>
    </div>
</div>
 *@